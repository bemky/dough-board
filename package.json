{
  "name": "dough-board",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:watch": "node esbuild.config.mjs --watch"
  },
  "dependencies": {
    "esbuild": "^0.25.12",
    "komps": "github:bemky/komps"
  }
}
