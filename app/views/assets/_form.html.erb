<% field_classes = "block w-full rounded-md border border-gray-300 px-3 py-2 text-sm focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500" %>
<% label_classes = "block text-sm font-medium text-gray-700 mb-1" %>
<%= form_for asset do |f| %>
    <div class="space-y-4">
        <% if asset.errors.any? %>
            <div class="rounded-md border border-red-300 bg-red-50 px-3 py-2 text-sm text-red-700">
                <ul class="list-inside list-disc">
                    <% asset.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                    <% end %>
                </ul>
            </div>
        <% end %>
        <div>
            <%= f.label :symbol, class: label_classes %>
            <%= f.text_field :symbol, class: field_classes %>
        </div>
        <div>
            <%= f.label :name, class: label_classes %>
            <%= f.text_field :name, class: field_classes %>
        </div>
        <div>
            <%= f.label :type, class: label_classes %>
            <%= f.select :type, %w(stock fund crypto), {include_blank: true}, class: field_classes %>
            <p class="mt-1 text-xs text-gray-500">Crypto is quoted as BINANCE:&lt;SYMBOL&gt;USDT.</p>
        </div>
        <div>
            <%= f.label :exchange, class: label_classes %>
            <%= f.collection_select :exchange_id, Exchange.order(:code), :id, :label, {include_blank: true}, class: field_classes %>
        </div>
        <button type="submit" class="inline-flex items-center justify-center rounded-md bg-green-600 px-4 py-2 text-sm font-semibold text-white hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2">
            <%= asset.persisted? ? "Save" : "Add" %>
        </button>
    </div>
<% end %>
